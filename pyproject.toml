[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "livekit-ai-note-taker"
version = "0.1.2"
description = "LiveKit agent that transcribes meetings with Deepgram and streams rolling summaries via Groq/OpenRouter/OpenAI."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Proprietary" }
authors = [{ name = "UrCalls" }]
dependencies = [
  "livekit==1.0.19",
  "livekit-agents==1.3.2",
  "livekit-api==1.0.7",
  "livekit-blingfire==1.0.0",
  "livekit-plugins-deepgram==1.3.2",
  "livekit-plugins-openai==1.3.2",
  "livekit-protocol==1.1.0",
  "opentelemetry-api>=1.34.1,<1.39",
  "opentelemetry-sdk>=1.34.1,<1.39",
  "opentelemetry-exporter-otlp-proto-http>=1.34.1,<1.39",
  "opentelemetry-exporter-otlp-proto-grpc>=1.34.1,<1.39",
  "aiohttp>=3.9",
  "python-dotenv>=1.0",
  "groq>=0.6",
  "httpx>=0.27",
  "requests>=2.31",
]

[project.scripts]
livekit-summarizer-worker = "livekit_meeting_summarizer.runner:main"

[project.urls]
Homepage = "https://github.com/urcalls/livekit-meeting-summarizer"
Issues = "https://github.com/urcalls/livekit-meeting-summarizer/issues"

[tool.setuptools.packages.find]
where = ["src"]

